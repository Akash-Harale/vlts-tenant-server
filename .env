# ─────────────────────────────────────────────
# VLTS-Tenant Server Configuration
# ─────────────────────────────────────────────

# MongoDB — same database as main backend (shared collections)
# MONGO_URI=mongodb+srv://akash:harale@cluster0.01kxxgp.mongodb.net/vlts_test?appName=Cluster0
MONGO_URI=mongodb+srv://nutantek:123Delhi@cluster0.1bg9msl.mongodb.net/vlts_poc?appName=Cluster0

# Server port — use 3006 to avoid conflict with main backend (3005)
PORT=3006

# CORS — allow tenant frontend origin(s)
CORS_ORIGIN=http://localhost:5173,http://localhost:5174,http://localhost:3000

# JWT Secrets — must match main backend so tokens are interoperable
JWT_SECRET=nutanTek123!
JWT_REFRESH_SECRET=tekNutan321!

# Transaction retry config
TRANSACTION_MAX_RETRIES=3
TRANSACTION_BACKOFF_MS=100

ALLOWED_ORIGINS=http://localhost:5173